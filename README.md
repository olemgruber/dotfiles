Copy following code to the root .zshrc after clonning 'dotfiles' repo to your /git folder

```
export DIR=~/git/dotfiles/;
source $DIR.zshrc;
```

My dotfiles repo is open at: https://github.com/bernardolemgruber/dotfiles
